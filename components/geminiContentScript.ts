import { getElementById } from "domutils";

/**
 * gemini のページで実行されるコンテンツスクリプト
 * プロンプトの入力中に、上矢印キーを押すと、履歴を遡る
 * @param {Document} document
 */
export function geminiContentScript(document: Document) {
	// ...
}
